<%= form_tag(requests_path, method: :delete) do %>
	<%= hidden_field_tag(:user_id, user.id) %>
	<%= hidden_field_tag(:group_id, group.id) %>
	<%= submit_tag('delete request', class: 'button tiny alert') %>
<% end %>